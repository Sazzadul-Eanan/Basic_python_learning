# 'Comprehensive list' makes code more clear and understandable

# [ 'expression' for 'item' in list ] .....is the format of a 'comprehensive list'

# why list ? because the whole code is written inside a 'list'

'''
# list comprehension of a 'map function' code (see again) program-66

num = [1, 2, 3, 4, 5]
result = [x*x for x in num]
print(result)

'''

# list comprehension of a 'filter function' code (see again) program-67

num = [1, 2, 3, 4, 5]
result = [x for x in num if x % 2 == 0]
print(result)

